import { users } from "../models/user.model";
import { agents } from "../models/agent.model";
import { buys } from "../models/buy.model";
import { rents } from "../models/rent.model";
import { properties } from "../models/property.model";
import { blogs } from "../models/blog.model";
import { companies } from "../models/company.model";

export const db =
{
    users,
    agents,
    buys,
    rents,
    properties,
    blogs,
    companies
}